import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler disabled — babel-plugin-react-compiler has resolution issues with Next.js 16 bundled babel
  // reactCompiler: true,
};

export default nextConfig;
